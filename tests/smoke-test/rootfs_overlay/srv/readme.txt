This is a sample Erlang release so that the scrub_otp_release.sh script can be
exercised.

It's a normal release with some files pruned out for size. It also has an
x86_64 executable in a priv directory that checks the `.noscrub` feature.
